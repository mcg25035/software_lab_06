#include <stdio.h>

int main() {
    printf("=== C Language Calculator ===\n");

    // Addition (Role B)
    int a = 10;
    int b = 20;
    printf("Addition: %d + %d = %d\n", a, b, a + b);

    // Subtraction (Role C)
    int x = 50;
    int y = 30;
    printf("Subtraction: %d - %d = %d\n", x, y, x - y);

    printf("System Check OK\n");
    return 0;
}
